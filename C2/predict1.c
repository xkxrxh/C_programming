#include <stdio.h>

int main() {
    int x= 10, y = 20;
     if(x==y);  // when semicolon come after if statement that means the if sytatemnet has ended there only.
       printf("%d %d,x,y");

    return 0;
}