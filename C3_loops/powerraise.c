#include <stdio.h>

int main() {
    
    int n1;
    printf("Enter the number:");
    scanf("%d", &n1);

    int power = 1;
    for(int i=n1;i<=n1;i++){
        power = power*n1;

    }
    printf("%d raised to the power of %sd is %d", n1,b,power);

    return 0;
}